export const test = (req: any, res: any, next: any) => {
    // const { file, body } = req;
    // console.log('file: ', file);
    // console.log('body: ', body);
    // res.json({ posterName: file.filename });
    // res.end(file.filename);
    // res.status(200).json({ message: 'ok' });
};
